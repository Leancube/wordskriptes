using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class heloyword : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        // вывод в консоль 
        print("привет мир");
            }
    // Update is called once per frame
    void Update()
    {
        
    }
}
